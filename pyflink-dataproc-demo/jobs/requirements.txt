# PyFlink dependencies for DataProc
# Note: PyFlink is already included with Flink component on DataProc
# These are additional dependencies that might be needed for specific use cases

# Data processing libraries
pandas>=1.3.0
numpy>=1.21.0

# Google Cloud libraries (if needed for custom connectors)
google-cloud-storage>=2.0.0,<3.0.0
google-cloud-bigquery>=3.0.0

# Logging and monitoring
structlog>=21.0.0

# Optional: For custom serialization
pyarrow>=5.0.0

# Optional: For JSON processing
jsonschema>=4.0.0
